# Rust CLI example — build the binary before testing.

[provider]
step_timeout_secs = 120
agent_args = [
    "--dangerously-skip-permissions",
    "--no-session-persistence",
    "--model",
    "haiku",
    "--effort",
    "medium",
]

[commands.build]
kind = "short_lived"
cmd = "cargo build"
