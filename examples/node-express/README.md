# Node Express Example

A TypeScript Express app with health, todo API, and HTML endpoints. Demonstrates test discovery, `include_only` shared setup, and `pnpm install` as a short-lived command.

## Run

```sh
bugatti test
```
